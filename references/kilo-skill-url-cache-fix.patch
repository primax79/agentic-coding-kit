diff --git a/packages/opencode/src/kilocode/skill-remove.ts b/packages/opencode/src/kilocode/skill-remove.ts
index d541f33e92..d1e709bbb6 100644
--- a/packages/opencode/src/kilocode/skill-remove.ts
+++ b/packages/opencode/src/kilocode/skill-remove.ts
@@ -1,4 +1,4 @@
-import { unlink } from "node:fs/promises"
+import { rm, unlink } from "node:fs/promises"
 import path from "node:path"
 import { Global } from "@opencode-ai/core/global"
 import { Skill } from "@/skill"
@@ -22,16 +22,29 @@ export function target(location: string, skills: readonly Info[]) {
   const file = path.resolve(skill.location)
   if (path.basename(file) !== "SKILL.md") throw new Error("skill location must reference SKILL.md")
 
+  return file
+}
+
+// Every file under a cache-backed skill's directory was written by the
+// skills.urls downloader (discovery.ts) — unlike a local/project skill
+// directory, there's nothing else in there to preserve, so the whole
+// directory can be removed instead of just unlisting the skill.
+function cacheRootFor(file: string): string | undefined {
   const cache = path.join(Global.Path.cache, "skills")
   const relative = path.relative(cache, file)
-  if (relative === "" || (!relative.startsWith("..") && !path.isAbsolute(relative))) {
-    throw new Error("remove URL-backed skills from configuration")
-  }
-  return file
+  const insideCache = relative !== "" && !relative.startsWith("..") && !path.isAbsolute(relative)
+  return insideCache ? path.dirname(file) : undefined
 }
 
 export async function remove(location: string, skills: readonly Info[]) {
   const file = target(location, skills)
+
+  const cacheDir = cacheRootFor(file)
+  if (cacheDir) {
+    await rm(cacheDir, { recursive: true, force: true })
+    return
+  }
+
   // Removing only the manifest disables discovery without recursively deleting user files.
   await unlink(file)
 }
diff --git a/packages/opencode/src/skill/index.ts b/packages/opencode/src/skill/index.ts
index 3f90f6be87..044ce4996c 100644
--- a/packages/opencode/src/skill/index.ts
+++ b/packages/opencode/src/skill/index.ts
@@ -78,6 +78,10 @@ export class NotFoundError extends Schema.TaggedErrorClass<NotFoundError>()("Ski
 type State = {
   skills: Record<string, Info>
   dirs: Set<string>
+  // kilocode_change start - collision precedence: never let a skills.urls
+  // cache entry silently clobber a skill loaded from any other source
+  fromCache: Record<string, boolean>
+  // kilocode_change end
 }
 
 // kilocode_change start - retain markdown trust provenance through discovery
@@ -86,6 +90,7 @@ type Match = {
   trusted: boolean
   root?: string
   sourceRoot?: string
+  fromCache?: boolean // kilocode_change - set only for skills.urls-pulled matches
 }
 
 type DiscoveryState = {
@@ -137,15 +142,33 @@ const add = Effect.fnUntraced(function* (state: State, match: Match, events: Eve
 
   if (!isSkillFrontmatter(md.data)) return
 
-  if (state.skills[md.data.name]) {
+  // kilocode_change start - a skills.urls cache entry must never silently
+  // replace a skill already loaded from any non-cache source: the cache has
+  // no invalidation (discovery.ts's download() skips any file that already
+  // exists) and no supported removal, so letting it win a name collision
+  // means a stale copy can shadow a deliberately (re)installed skill with
+  // only a log line as evidence. Non-cache-vs-non-cache collisions keep the
+  // existing last-scanned-wins behavior unchanged.
+  const existing = state.skills[md.data.name]
+  if (existing) {
+    const existingFromCache = state.fromCache[md.data.name] ?? false
+    if (!existingFromCache && match.fromCache) {
+      yield* Effect.logWarning(
+        "duplicate skill name - ignoring skills.urls cache match, keeping existing non-cache entry",
+        { name: md.data.name, kept: existing.location, ignored: match.path },
+      )
+      return
+    }
     yield* Effect.logWarning("duplicate skill name", {
       name: md.data.name,
-      existing: state.skills[md.data.name].location,
-      duplicate: match.path, // kilocode_change
+      existing: existing.location,
+      duplicate: match.path,
     })
   }
+  // kilocode_change end
 
   state.dirs.add(path.dirname(match.path)) // kilocode_change
+  state.fromCache[md.data.name] = match.fromCache ?? false // kilocode_change
   state.skills[md.data.name] = {
     name: md.data.name,
     description: md.data.description,
@@ -158,7 +181,14 @@ const scan = Effect.fnUntraced(function* (
   state: ScanState,
   root: string,
   pattern: string,
-  opts?: { dot?: boolean; scope?: string; trusted?: boolean; root?: string; sourceRoot?: string }, // kilocode_change
+  opts?: {
+    dot?: boolean
+    scope?: string
+    trusted?: boolean
+    root?: string
+    sourceRoot?: string
+    fromCache?: boolean // kilocode_change
+  },
 ) {
   const matches = yield* Effect.tryPromise({
     try: () =>
@@ -186,6 +216,7 @@ const scan = Effect.fnUntraced(function* (
       trusted: opts?.trusted ?? false,
       root: opts?.root,
       sourceRoot: opts?.sourceRoot,
+      fromCache: opts?.fromCache, // kilocode_change
     })
     // kilocode_change end
     state.dirs.add(path.dirname(match))
@@ -273,7 +304,9 @@ const discoverSkills = Effect.fnUntraced(function* (
   for (const url of cfg.skills?.urls ?? []) {
     const pulledDirs = yield* discovery.pull(url)
     for (const dir of pulledDirs) {
-      yield* scan(state, dir, SKILL_PATTERN, { root: dir }) // kilocode_change - downloaded markdown is untrusted
+      // kilocode_change - downloaded markdown is untrusted, and flagged fromCache so a
+      // stale cache entry can never win a name collision against a non-cache skill
+      yield* scan(state, dir, SKILL_PATTERN, { root: dir, fromCache: true })
     }
   }
 
@@ -332,7 +365,7 @@ export const layer = Layer.effect(
     )
     const state = yield* InstanceState.make(
       Effect.fn("Skill.state")(function* () {
-        const s: State = { skills: {}, dirs: new Set() }
+        const s: State = { skills: {}, dirs: new Set(), fromCache: {} } // kilocode_change
         yield* loadSkills(s, yield* InstanceState.get(discovered), events)
         return s
       }),
diff --git a/packages/opencode/test/kilocode/skill-remove.test.ts b/packages/opencode/test/kilocode/skill-remove.test.ts
index 1a259d769a..74c94edf59 100644
--- a/packages/opencode/test/kilocode/skill-remove.test.ts
+++ b/packages/opencode/test/kilocode/skill-remove.test.ts
@@ -1,7 +1,8 @@
 import { describe, expect, test } from "bun:test"
+import { mkdir, readdir, rm, writeFile } from "node:fs/promises"
 import path from "node:path"
 import { Global } from "@opencode-ai/core/global"
-import { target } from "../../src/kilocode/skill-remove"
+import { remove, target } from "../../src/kilocode/skill-remove"
 
 const info = (location: string) => ({
   name: "synthetic",
@@ -34,9 +35,23 @@ describe("skill removal target", () => {
     expect(() => target(location, [info(location)])).toThrow("skill location must reference SKILL.md")
   })
 
-  test("rejects URL-backed cache entries", () => {
-    const location = path.join(Global.Path.cache, "skills", "synthetic", "SKILL.md")
-    expect(() => target(location, [info(location)])).toThrow("remove URL-backed skills from configuration")
+  test("removes a URL-backed cache skill's entire directory, not just the manifest", async () => {
+    const cache = path.join(Global.Path.cache, "skills")
+    const skillDir = path.join(cache, `__kilo_synthetic_${Date.now()}__`)
+    await mkdir(skillDir, { recursive: true })
+    const skillMd = path.join(skillDir, "SKILL.md")
+    await writeFile(skillMd, "---\nname: synthetic\n---\nsynthetic")
+    // The downloader writes every manifest-listed file alongside SKILL.md —
+    // removal must take the sibling files with it, not just the manifest.
+    await writeFile(path.join(skillDir, "scripts.py"), "# sibling file the downloader also wrote")
+
+    try {
+      await remove(skillMd, [info(skillMd)])
+      const remaining = await readdir(cache).catch(() => [] as string[])
+      expect(remaining).not.toContain(path.basename(skillDir))
+    } finally {
+      await rm(skillDir, { recursive: true, force: true })
+    }
   })
 
   test("returns only the registered skill manifest", () => {
